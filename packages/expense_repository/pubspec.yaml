name: expense_repository
description: The Repo that handless expenses
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ^3.5.2

dependencies:
  flutter:
    sdk: flutter

  #equatable: ^2.0.5
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.3
  #firebase_storage: ^12.3.2
  #rxdart: ^0.28.0
  #path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0


flutter:
  uses-material-design: true
